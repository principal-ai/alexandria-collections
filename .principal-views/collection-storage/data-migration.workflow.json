{
  "version": "1.0.0",
  "canvas": ".principal-views/collection-storage/collection-storage.otel.canvas",
  "name": "Data Migration",
  "description": "Exporting and importing collection data for backup/restore or migration",
  "mode": "span-tree",
  "spanPattern": "storage.export|storage.import|storage.clear|storage.file.*",
  "scenarios": [
    {
      "id": "export-data",
      "priority": 1,
      "description": "User exports all collections and memberships",
      "template": {
        "summary": "Exported {{output.collectionsCount}} collections and {{output.membershipsCount}} memberships",
        "events": {
          "storage.export": "Exported {{output.collectionsCount}} collections and {{output.membershipsCount}} memberships in {{duration.ms}}ms",
          "storage.file.read": "Read {{file.name}}"
        }
      }
    },
    {
      "id": "import-data",
      "priority": 2,
      "description": "User imports collections and memberships with merge strategy",
      "template": {
        "summary": "Imported {{output.collectionsImported}} collections and {{output.membershipsImported}} memberships using {{input.strategy}} strategy",
        "events": {
          "storage.import": "Imported {{input.collectionsCount}} collections, {{input.membershipsCount}} memberships using \"{{input.strategy}}\" strategy (result: {{output.collectionsImported}}/{{output.membershipsImported}} imported) in {{duration.ms}}ms",
          "storage.file.read": "Read {{file.name}}",
          "storage.file.write": "Wrote {{items.count}} items to {{file.name}}"
        }
      }
    },
    {
      "id": "clear-all-data",
      "priority": 3,
      "description": "User clears all collections and memberships",
      "template": {
        "summary": "Cleared {{output.collectionsCleared}} collections and {{output.membershipsCleared}} memberships",
        "events": {
          "storage.clear": "Cleared {{output.collectionsCleared}} collections and {{output.membershipsCleared}} memberships in {{duration.ms}}ms",
          "storage.file.write": "Wrote {{items.count}} items to {{file.name}}"
        }
      }
    }
  ]
}
